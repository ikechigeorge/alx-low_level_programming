#include "main.h"

/**
 * mul - A function that multiples two integers
 *
 * @a: char to multiply with the other char
 * @b: char to multiply with the other char
 *
 * Return: 0 always successful
 *
 */
int mul(int a, int b)
{
	return (a * b);
}
